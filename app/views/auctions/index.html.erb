<h1>All Current Auction </h1>
<ul>
  <% @auctions.each do |auction| %>
    <h3><li><%= auction.title %></li></h3>
    <span> <%= link_to 'View this auction', auction_path(auction.id)%></span>
  <% end %>
</ul>
